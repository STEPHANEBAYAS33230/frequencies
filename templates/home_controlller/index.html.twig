{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} | Accueil{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('assets/css/bootstrap.min.css') }}">
{% endblock %}

{% block scriptjs %}
    <script src="{{ asset('assets/js/bootstrap.bundle.min.js') }}"></script>
{% endblock %}

{% block header %}
{% endblock %}

{% block main %}
    {% if ( not(is_granted('ROLE_INTER'))) %}
        <div id="total">
        <div class="container" id="blalba" >
            <div id="navBar1" class="row justify-content-around col-12">
                <h1 id="anglaisTitreText1">FREQUENCIES</h1>
                <p id="suite1">
                    <a id="menuOption1" href="#">Accueil</a></p>
                <p id="suite2">
                    <a id="menuOption2" href="#">Présentation</a>
                </p>
                <p id="suite3">
                    <a id="menuOption3" href="#">Nos séries</a>
                </p>
                <p id="suite4">
                    <a id="menuOption4" href="#">Contact</a>
                </p>
                <p id="suite6">
                    <a id="menuOption5" href="{{ path('app_loginEmail') }}">Se connecter</a>
                </p>
            </div> <!-- fin row -->
        </div>
            <div id="suite7">
                <h1 id="TitreText2">Présentation de l'association : </h1>
                <p id="Paragraphe1">Frequencies produit des fictions sonores, créées et inspirées par des personnes malvoyantes à destination de tous. Nous créons cette fiction avec l’objectif de faire rêver les gens en leur racontant une histoire qui leur permette de s’évader de leur quotidien. Notre projet repose sur une démarche inclusive : nous sommes en discussion avec des structures spécialisées pour associer à nos séries de fiction des personnes en situation de handicap. Derrière le micro l'handicap n'existe plus, dans une fiction sonore on peux être qui on veut, seul la voix compte. </p>
            </div>
        <div id="suite8">
            {% for label, messages in app.flashes %}
                {% for message in messages %}
                    <div id="suite11" class="text-warning">
                        {{ message }}
                    </div>
                {% endfor %}
            {% endfor %}
        </div>
        <div id="suite9">
            <h1 id="TitreText3">Nos séries en ligne</h1>
            {% for serie in series %}
                <div id="suite{{ 5000+random(10000) }}" class="bg-warning">
                    <h1 id="Paragraphe{{ serie.id*7*random(1000,2500) }}">Notre série numero {{ serie.id }} : {{ serie.nom }}</h1></h1>
                    {% for episode in episodes %}
                        {% if serie==episode.serie %}
                            <p id="Paragraphe{{ episode.numero*70*random(1000,2500) }}">Episode N°{{ episode.numero }} :{{ episode.nomEpisode }}</p>
                            <audio id="sonnore{{ episode.id }}" src="{{ asset('uploads/sonnores/' ~ episode.brochureFilename) }}" controls preload="auto" >
                                Votre navigateur ne supporte pas l'élément <code>audio</code>.
                            </audio> <br>
                        {% endif %}
                    {% endfor %}
                </div>
                <div>
                    <h1><hr></h1>
                </div>
            {% endfor %}
        </div>
        </div>
    {% endif %}
    {# src="{{ asset('assets/js/acessVoice.js') }}" #}
    <script  src="{{ asset('assets/js/acessVoice.js') }}" type="text/javascript">
    </script>
{% endblock %}